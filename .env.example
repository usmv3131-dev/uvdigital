# Application environment (development|production)
APP_ENV=development

# Port exposed by the web application
PORT=3000

# Database connection parameters (set if a database is introduced)
DB_HOST=localhost
DB_PORT=5432
DB_USER=uvdigital
DB_PASSWORD=change-me
DB_NAME=uvdigital
