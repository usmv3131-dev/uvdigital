services:
  app:
    image: ${DOCKER_IMAGE:?ghcr.io/owner/repo:latest}
    container_name: uvdigital-app
    environment:
      APP_ENV: ${APP_ENV:-production}
      PORT: ${APP_PORT:-8080}
    ports:
      - "${HOST_PORT:-3000}:8080"
    restart: unless-stopped
