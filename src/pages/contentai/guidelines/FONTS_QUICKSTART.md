# ⚡ Шрифты — Quick Start

## 🎯 Быстрый старт

### Используемые шрифты

| Элемент | Шрифт | Вес | Letter-spacing |
|---------|-------|-----|----------------|
| **h1** | Manrope | 800 | -0.03em |
| **h2** | Manrope | 700 | -0.02em |
| **h3** | Manrope | 700 | -0.015em |
| **h4** | Manrope | 600 | -0.01em |
| **p** | Inter | 400 | 0.005em |
| **label** | Inter | 500 | 0.002em |
| **button** | Manrope | 600 | 0.005em |

---

## 📝 Как использовать

### В компонентах

```tsx
// ✅ Заголовок — используйте style
<h1 style={{ fontFamily: 'var(--font-heading)' }}>
  Content AI
</h1>

// ✅ Текст — используйте style
<p style={{ fontFamily: 'var(--font-body)', letterSpacing: '0.01em' }}>
  AI-контент бот для салонов красоты
</p>

// ✅ Кнопка — font применяется автоматически
<button className="px-8 py-4 rounded-2xl">
  Запустить бесплатно
</button>
```

### Для inline styles

```tsx
// Заголовок секции
style={{ fontFamily: 'var(--font-heading)' }}

// Описание
style={{ fontFamily: 'var(--font-body)', letterSpacing: '0.01em' }}

// Кнопка (опционально, уже применён через base)
style={{ fontFamily: 'var(--font-button)' }}
```

---

## 🎨 CSS переменные

```css
--font-heading: 'Manrope', system-ui, sans-serif;
--font-body: 'Inter', system-ui, sans-serif;
--font-button: 'Manrope', system-ui, sans-serif;
```

---

## 📦 Что уже настроено?

✅ Google Fonts импорт в `globals.css`  
✅ CSS переменные для всех шрифтов  
✅ Base typography для h1-h4, p, label, button  
✅ Правильные веса и letter-spacing  
✅ Line-heights оптимизированы  

---

## 🚀 Ничего не нужно делать!

Все шрифты уже применяются автоматически через `@layer base` в `globals.css`.

**Просто используйте:**
- `<h1>`, `<h2>`, `<h3>`, `<h4>` — автоматически Manrope
- `<p>`, `<label>` — автоматически Inter
- `<button>` — автоматически Manrope SemiBold

**Для кастомных стилей добавьте:**
```tsx
style={{ fontFamily: 'var(--font-heading)' }}
```

---

## 📊 Тестирование

Используйте компонент `TypographyShowcase` для проверки:

```tsx
import { TypographyShowcase } from './components/TypographyShowcase';

// В любом месте
<TypographyShowcase />
```

Он покажет все варианты типографики с примерами.

---

**Всё готово! Просто пишите HTML и шрифты применятся автоматически.** ✨
